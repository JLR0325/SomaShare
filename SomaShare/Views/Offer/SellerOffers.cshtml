@model IEnumerable<Offer>

@{
    ViewData["Title"] = "Seller Offers";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <h1>Offers Received</h1>
            <p class="text-muted">Offers made on your textbooks</p>
            <hr />
        </div>
    </div>

    @if (Model != null && Model.Any())
    {
        <div class="row">
            <div class="col-12">
                <div class="table-responsive">
                    <table class="table table-striped table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th>Buyer</th>
                                <th>Offer Amount</th>
                                <th>Status</th>
                                <th>Date</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var offer in Model)
                            {
                                <tr>
                                    <td>Buyer #@offer.BuyerId</td>
                                    <td>$@offer.Amount.ToString("F2")</td>
                                    <td><span class="badge bg-warning text-dark">Pending</span></td>
                                    <td>@offer.CreatedAt.ToString("d")</td>
                                    <td>
                                        <a href="@Url.Action("Details", "Offer", new { id = offer.Id })" class="btn btn-sm btn-primary">Review</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="alert alert-info" role="alert">
            <i class="fas fa-info-circle"></i> You haven't received any offers yet.
        </div>
    }
</div>
